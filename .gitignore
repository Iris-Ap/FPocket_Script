# Per-protein run outputs (everything the pipeline generates)
runs/

# Downloaded structures (pipeline.sh re-fetches these from the PDB)
*.pdb

# fpocket output (regenerated on every run)
protein_clean_out/

# Generated figures / snapshots
*.png

# macOS clutter
.DS_Store

# Obsidian config (if a vault is opened here)
.obsidian/

# Local results — teammates regenerate these by running the pipeline
raw_FTMAP_results/
raw_FPOCKET_results/
ftmap_zips/
